---
import type { AthleteProfile } from '../../lib/types';
interface Props { positions: AthleteProfile['positions']; }
const { positions } = Astro.props;
---
<section>
  <h2>Positions</h2>
  <ul>{positions.map((p) => (<li>{p.name}{p.primary ? ' (primary)' : ''}</li>))}</ul>
</section>
