---
import type { AthleteProfile } from '../../lib/types';
interface Props { film: AthleteProfile['film']; }
const { film } = Astro.props;
---
<section>
  <h2>Film</h2>
  <p><a href={film.hudlWatch}>{film.title || 'Watch film'}</a></p>
</section>
